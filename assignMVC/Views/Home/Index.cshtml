@model DemoApp.Models.CommonModel
    <html>
        <head>
            <title>
                DemoApp
            </title>
        </head>
        <body>
            <h1>
                Products
            </h1>
            <table border="1">
                <tr>
                    <th>Product Id</th>
                    <th>Price</th>
                    <th>Stock</th>
                </tr>
                @foreach(var product in Model.productInfo)
                {
                    <tr>
                        <td>@product.Productid</td>
                        <td>@product.Price</td>
                        <td>@product.stock</td>
                    </tr>
                }
            </table>
            <table border="1" style="">
                <tr>
                    <th>OrderNo</th>
                    <th>Date</th>
                    <th>Customer Id</th>
                    <th>Product No</th>
                    <th>Quantity</th>
                </tr>
                @foreach(var product in Model.orderInfo)
                {
                    <tr>
                        <td>@product.orderno</td>
                        <td>@product.Date</td>
                        <td>@product.customerid</td>
                        <td>@product.productno</td>
                        <td>@product.quantity</td>
                    </tr>
                }
            </table>
            <form method="POST" action="/Home/Register">
                <label>Product:</label>
                <input type="number" name="id">
                <label>Price:</label>
                <input type="number" name="Price">
                <label>Stock:</label>
                <input type="number" name="stock">
                <input type="submit" value="Register">
            </form>
        </body>
    </html>